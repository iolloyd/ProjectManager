A simple one-page cgi script to use as a project manager. This was just an idea to see if I could create something simple enough to fit in just a single page. I made two versions, one in ruby and one in python.
